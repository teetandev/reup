# infra

Deployment and infrastructure assets: systemd units, nginx configs, Cloudflare
Tunnel config, and free-first hosting notes.

Reference templates live in `templates/` (`systemd/reup-agent.service`,
`nginx/agent-node.conf`, `env/*.example`). This is a Phase 01 placeholder.
