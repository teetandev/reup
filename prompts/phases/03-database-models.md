# Phase 03 — Database Models

```text
Implement DB models and migrations based on docs/specs/DATABASE_SCHEMA.md.

Use SQLAlchemy/SQLModel/Alembic or the chosen stack consistently.

Models:
- users
- api_keys
- vps_nodes
- jobs
- job_events
- admin_audit_logs

Add enums and indexes.

Do not implement UI.
```
