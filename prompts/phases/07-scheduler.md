# Phase 07 — Scheduler

```text
Implement scheduler based on docs/specs/SCHEDULER_SPEC.md.

Requirements:
- user creates job
- check user limits
- find idle fresh node
- lock node transactionally
- create upload token
- return upload_url
- release node on terminal status

Test race condition if possible.
```
